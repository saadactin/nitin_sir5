CREATE PROCEDURE "DP_INSTALL" () LANGUAGE SQLSCRIPT  SQL SECURITY INVOKER  AS BEGIN     DECLARE maxId INT;     SELECT (IFNULL(MAX("AbsEntry"), 0) + 1) INTO maxId FROM ODPH;     EXEC 'CREATE SEQUENCE DP_TEMPLATE_ID_SEQ START WITH ' || :maxId; END
